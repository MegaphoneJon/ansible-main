- name: Show the site in question
  debug:
    msg: Extensions for "{{ item.item.key }}"

- name: deleteme
  debug:
    var: item.stdout

- debug: 
    msg: "{{ inner_item.name }} {{ inner_item.version }}"
  with_items: "{{ item.stdout | from_json | list }}"
  loop_control:
    loop_var: inner_item

