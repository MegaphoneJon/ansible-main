---
# This provisions any VPSes that were defined but not built in server-list
- hosts: yellow.megaphonetech.com

  tasks:
#  - set_fact:
#      newpass: "{{ lookup('passwordstore', 'deleteme create=true overwrite=yes length=320 chars=ascii_letters,digits') }}"
#      newpass: "{{ lookup('passwordstore', 'deleteme create=true overwrite=yes length=320 chars=ascii_letters,digits,.,:,-,_') }}"
#      newpass: "{{ lookup('passwordstore', 'deleteme create=true overwrite=yes length=320 chars=ascii_letters,digits,punctuation') }}"
#      newpass: "{{ lookup('passwordstore', 'deleteme create=true overwrite=yes length=320') }}"

  - debug: var=ansible_date_time
