linode_personal_access_token: "{{ lookup('passwordstore', 'joemcl/linode/personal_access_token') }}"
users:
  Jon:
    name: jon
    shell: "/bin/zsh"
    groups:
      - sudo
      - www-data
    password: "{{ lookup('passwordstore', 'megaphone/ansible/users/jon') }}"
    authorized:
      - files/jon_id_rsa.pub
    hasdotfiles: true
    dotfiles: https://github.com/MegaphoneJon/dotfiles.git
  Joseph:
    name: joseph
    shell: "/bin/bash"
    groups:
      - sudo
      - www-data
    password: "{{ lookup('passwordstore', 'megaphone/ansible/users/joseph') }}"
    authorized:
      - files/joseph_id_rsa.pub
    hasdotfiles: false
  Joe:
    name: joe
    shell: "/bin/bash"
    groups:
      - sudo
      - www-data
    authorized:
      - files/joe_id_rsa.pub
    hasdotfiles: false

